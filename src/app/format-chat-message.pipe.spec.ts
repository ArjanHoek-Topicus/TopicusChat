import { FormatChatMessagePipe } from './format-chat-message.pipe';

describe('FormatChatMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatChatMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
